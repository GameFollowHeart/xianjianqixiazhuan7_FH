### 游戏数值读取代码案例
```
.版本 2
.支持库 ncdx98
.支持库 spec

.程序集 窗口程序集_启动窗口
.程序集变量 内存, x64进程内存
.程序集变量 地址, 长整数型
.程序集变量 进程id, 长整数型
.程序集变量 句柄, 整数型
.程序集变量 地址x, 模块地址_
.程序集变量 modulePTR, 长整数型
.程序集变量 新当前生命值, 长整数型
.程序集变量 界石数量, 整数型
.程序集变量 新界石数量, 整数型
.程序集变量 界石地址, 长整数型

.子程序 __启动窗口_创建完毕


内存.提高权限 ()

进程id ＝ 进程_名取id (“轮回修仙路.exe”, )
调试输出 (“进程id：”, 进程id)
句柄 ＝ 内存.打开进程 (进程id)
调试输出 (“句柄：”, 句柄)

调试输出 (“原来 地址x：”, 地址x)
modulePTR ＝ 内存.取模块地址Ex (句柄, “GameAssembly.dll”, 地址x)
调试输出 (“地址x：”, 地址x)
调试输出 (“modulePTR 地址：”, modulePTR)
地址 ＝ 内存.读长整数型 (句柄, modulePTR ＋ 十六到十_ (“01F454D8”))
调试输出 (“地址：”, 地址)
调试输出 (“地址x.模块地址：”, 地址x.模块地址)
调试输出 (“十到十六_ (地址x.模块地址, )：”, 十到十六_ (地址x.模块地址, ))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“40”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“B8”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“0”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“28”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“78”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“70”))
地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 十六到十_ (“268”))
界石地址 ＝ 地址 ＋ 十六到十_ (“18”)
界石数量 ＝ 内存.读整数型 (句柄, 界石地址)  ' 最后一个偏移量读取是一定要选择对读取类型，不然读取出来的数据会不一样'

调试输出 (“此时 地址：”, 地址)
' 调试输出 (“来自cheatEngine的地址，当前界石数量：”, 内存.读整数型 (句柄, 十六到十_ (“1B1344BC818”)))'
调试输出 (“这里计算偏移量的来的，当前界石数量：”, 界石数量)
新界石数量 ＝ 602
调试输出 (“尝试修改界石数量：”, 新界石数量)
内存.写整数型 (句柄, 界石地址, 新界石数量)
界石数量 ＝ 内存.读整数型 (句柄, 界石地址)
调试输出 (“修改后的，再读取的 界石数量：”, 界石数量)


' 调试输出 (“--------------------------当前生命值实验 这个 1B13443A7B0 是动态地址会变，通过偏移量计算所得才是最可靠的-------------------------------”)
' 当前生命值 ＝ 内存.读长整数型 (句柄, 十六到十_ (“1B13443A7B0”))
' 调试输出 (“当前生命值：”, 当前生命值)
' 新当前生命值 ＝ 600002
' 调试输出 (“尝试修改生命值：”, 新当前生命值)
' 内存.写整数型 (句柄, 十六到十_ (“1B13443A7B0”), 新当前生命值)
' 当前生命值 ＝ 内存.读长整数型 (句柄, 十六到十_ (“1B13443A7B0”))
' 调试输出 (“修改后的，再读取的 当前生命值：”, 当前生命值)
内存.关闭句柄 (句柄)

```


### 自定义可复用的子程序案例
```
.版本 2
.支持库 ncdx98

.子程序 游戏数值最终地址读取, 长整数型, , 模块文件全名=“GameAssembly.dll”；模块偏移地址=“01F454D8” --> 模块文件全名 和 模块偏移地址 对应的就是 CheatEngine 里指针的 "GameAssembly.dll"+01F454D8；偏移量数组=“[40,B8,0,28,78,70,28,18]” --> 是cheatEngine里指针里从下网上对应的偏移量；
.参数 模块文件全名, 文本型
.参数 模块偏移地址, 文本型
.参数 偏移量数组, 长整数型, 数组
.局部变量 内存, x64进程内存
.局部变量 地址, 长整数型
.局部变量 进程id, 长整数型
.局部变量 句柄, 整数型
.局部变量 模块基址, 长整数型
.局部变量 最终地址, 长整数型, , , 十进制
.局部变量 偏移量数组成员数, 整数型
.局部变量 i, 整数型

' ' 模块文件全名 ＝ “GameAssembly.dll”
' ' 模块偏移地址=“01F454D8” --> 模块文件全名 和 模块偏移地址 对应的就是 CheatEngine 里指针的 "GameAssembly.dll"+01F454D8
' ' 偏移量数组=[40,B8,0,28,78,70,28,18] --> 是cheatEngine里指针里从下网上对应的偏移量
' 游戏数值最终地址读取 (模块文件全名, 模块偏移地址, 偏移量数组)
' 内存.提高权限 ()

' 进程id ＝ 进程_名取id (“轮回修仙路.exe”, )
' 调试输出 (“进程id：”, 进程id)
' 句柄 ＝ 内存.打开进程 (进程id)
' 调试输出 (“句柄：”, 句柄)

' 模块基址 ＝ 内存.取模块地址Ex (句柄, 模块文件全名, )
' 调试输出 (“模块基址：”, 模块基址)
' 地址 ＝ 内存.读长整数型 (句柄, 模块基址 ＋ 十六到十_ (模块偏移地址))
' 偏移量数组成员数 ＝ 取数组成员数 (偏移量数组)
.' 计次循环首 (偏移量数组成员数 － 1, i)
    ' 地址 ＝ 内存.读长整数型 (句柄, 地址 ＋ 偏移量数组 [i])
    ' 调试输出 (“i= ”, i)
.' 计次循环尾 ()
' 最终地址 ＝ 地址 ＋ 偏移量数组 [偏移量数组成员数]
' 调试输出 (“最终地址：”, 最终地址)
' 内存.关闭句柄 (句柄)
' 返回 (最终地址)

```